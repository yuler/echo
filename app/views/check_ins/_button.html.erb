<%= turbo_frame_tag "check_in_button_#{post.id}" do %>
  <% if Current.user&.checked_in?(post) %>
    <!-- Check-in completed state -->
    <div class="mt-8 mb-4 p-6 rounded-2xl bg-neutral-100 border border-neutral-200 text-center relative overflow-hidden group shadow-sm transition-all hover:shadow-md">
      <div class="relative z-10">
        <div class="mx-auto w-12 h-12 bg-green-500 rounded-full flex items-center justify-center mb-3 text-white shadow-sm ring-4 ring-green-100">
            <%= inline_svg "check.svg", class: "h-6 w-6" %>
        </div>
        <h3 class="text-heading-3 text-neutral-900 mb-1 font-semibold tracking-tight">Check-in Completed</h3>
        <p class="text-body-sm text-neutral-500 mb-5">You're making great progress! Current streak: <strong class="text-neutral-900 font-semibold"><%= Current.user.check_in_stats[:current_streak] %></strong> days</p>

        <button type="button" data-action="click->share#open" class="btn bg-neutral-900 hover:bg-neutral-800 text-white rounded-full px-7 py-2.5 transition-all font-medium shadow-sm hover:shadow focus-ring text-sm tracking-wide inline-flex items-center gap-2 relative overflow-hidden group">
          <%= inline_svg "share.svg", class: "w-4 h-4 opacity-70 group-hover:scale-110 transition-transform" %>
          Share Progress Poster
        </button>
      </div>
    </div>
  <% elsif Current.user %>
    <!-- Check in button -->
    <div class="my-8 text-center pt-6 pb-2">
      <%= button_to post_check_in_path(post), method: :post, class: "btn bg-neutral-900 hover:bg-neutral-800 text-white rounded-full px-8 py-3.5 w-full sm:w-auto font-medium shadow-sm hover:shadow-md transition-all group inline-flex items-center justify-center relative overflow-hidden focus-ring", data: { turbo: true } do %>
        <span class="relative z-10 flex items-center justify-center gap-2.5 text-[15px] tracking-wide">
          <%= inline_svg "check-badge.svg", class: "w-5 h-5 opacity-70 group-hover:rotate-[15deg] group-hover:scale-110 transition-all duration-300" %>
          Complete Check In
        </span>
      <% end %>
    </div>
  <% else %>
    <div class="my-8 text-center pt-6 pb-2 border-t border-neutral-100">
      <p class="text-body-sm text-neutral-500 mb-3">Check in to track your reading streaks</p>
      <%= link_to "Sign in to check in", new_session_path, class: "btn bg-neutral-100 hover:bg-neutral-200 text-neutral-700 rounded-full px-6 py-2.5 transition-colors font-medium text-sm" %>
    </div>
  <% end %>
<% end %>
